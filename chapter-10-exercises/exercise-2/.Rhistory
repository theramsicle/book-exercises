# Create a vector of 100 employees ("Employee 1", "Employee 2", ... "Employee 100")
# Hint: use the `paste()` function and vector recycling to add a number to the word
# "Employee"
employees <- paste("Employee", 1:100)
print(employees)
salaries_2017 <- runif(100,40000,50000)
# Create a vector of 100 annual salary adjustments between -5000 and 10000.
# (A negative number represents a salary decrease due to corporate greed)
# Again use the `runif()` function to pick 100 random numbers in that range.
adjustments <- runif(100, -5000, 10000)
salaries <- data.frame(employees, salaries_2017, adjustments)
# Add a column to the `salaries` data frame that represents each person's
# salary in 2018 (e.g., with the salary adjustment added in).
salaries
# Add a column to the `salaries` data frame that represents each person's
# salary in 2018 (e.g., with the salary adjustment added in).
salaries[[4]]
# Add a column to the `salaries` data frame that represents each person's
# salary in 2018 (e.g., with the salary adjustment added in).
salaries[[4]] <- f
# Add a column to the `salaries` data frame that represents each person's
# salary in 2018 (e.g., with the salary adjustment added in).
salaries[[4]] <- "f"
# Add a column to the `salaries` data frame that represents each person's
# salary in 2018 (e.g., with the salary adjustment added in).
salaries[[4]] <- salaries_2017 + adjustments
View(salaries)
# Add a column to the `salaries` data frame that represents each person's
# salary in 2018 (e.g., with the salary adjustment added in).
salaries[[4]] <- "salaries_2018" = salaries_2017 + adjustments
# Add a column to the `salaries` data frame that represents each person's
# salary in 2018 (e.g., with the salary adjustment added in).
salaries[[4]] <- ("salaries_2018" = salaries_2017 + adjustments)
View(salaries)
salaries[[4]] = "salaries_2018"
# Add a column to the `salaries` data frame that represents each person's
# salary in 2018 (e.g., with the salary adjustment added in).
salaries[[4]] <- salaries_2017 + adjustments
addcol(salaries$salaries_2018) <- salaries_2017 + adjustments
add_col(salaries$salaries_2018) <- salaries_2017 + adjustments
salaries$salaries_2018 <- salaries_2017 + adjustments
salaries$raise <- salaries$adjustments > 0
salaries["Employee 57", "salaries_2018"]
salaries[salaries$employees == "Employee 57", "salaries_2018"]
# How many employees got a raise?
salaries$raise <- TRUE
salaries$raise <- salaries$adjustments > 0
# How many employees got a raise?
got_raise <- salaries[salaries$raise == TRUE]
# How many employees got a raise?
got_raise <- salaries[salaries$raise == TRUE,]
length(got_raise)
View(got_raise)
length(got_raise$raise)
# How many employees got a raise?
nrow(salaries[salaries$raise == TRUE,])
# What was the dollar value of the highest raise?
salaries[salaries$adjustments == max()]
# What was the dollar value of the highest raise?
salaries[salaries$adjustments == max(salaries$adjustments)]
# What was the dollar value of the highest raise?
salaries[salaries$adjustments == max(salaries$adjustments),]
# What was the dollar value of the highest raise?
biggest_raise <- max(salaries$adjustments)
# What was the "name" of the employee who received the highest raise?
salaries[salaries$adjustments == max(salaries$adjustments),]
# What was the largest decrease in salaries between the two years?
pay_cut <- min(salaries$adjustments)
# What was the name of the employee who recieved largest decrease in salary?
salaries[salaries$adjustments == min(salaries$adjustments)]
# What was the largest decrease in salaries between the two years?
pay_cut <- min(salaries$adjustments)
# What was the name of the employee who recieved largest decrease in salary?
salaries[salaries$adjustments == min(salaries$adjustments),]
# What was the average salary change?
avg <- mean(salaries$adjustments)
# For people who did not get a raise, how much money did they lose on average?
pay_cutters <- salaries[salaries$adjustments < 0]
# For people who did not get a raise, how much money did they lose on average?
pay_cutters <- salaries[salaries$adjustments < 0,]
avg_paycut <- mean(pay_cutters$adjustments)
# What was the "name" of the employee who received the highest raise?
salaries[salaries$adjustments == max(salaries$adjustments), "adjustments"]
